import 'package:flutter/material.dart';

class AddInternalCardPage extends StatelessWidget {
  const AddInternalCardPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Thêm thẻ nội bộ'),
      ),
    );
  }
}
